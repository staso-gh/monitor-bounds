<Window x:Class="MonitorBounds.Views.RegionEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MonitorBounds.Views"
        mc:Ignorable="d"
        Title="Edit Protected Region" Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Region Name -->
        <Label Grid.Row="0" Grid.Column="0" Content="Name:" Margin="0,5"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name}" Margin="0,5"/>
        
        <!-- Left Position -->
        <Label Grid.Row="1" Grid.Column="0" Content="Left Position:" Margin="0,5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Left}" Margin="0,5"/>
        
        <!-- Top Position -->
        <Label Grid.Row="2" Grid.Column="0" Content="Top Position:" Margin="0,5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Top}" Margin="0,5"/>
        
        <!-- Width -->
        <Label Grid.Row="3" Grid.Column="0" Content="Width:" Margin="0,5"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Width}" Margin="0,5"/>
        
        <!-- Height -->
        <Label Grid.Row="4" Grid.Column="0" Content="Height:" Margin="0,5"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Height}" Margin="0,5"/>
        
        <!-- Active Status -->
        <Label Grid.Row="5" Grid.Column="0" Content="Active:" Margin="0,5"/>
        <CheckBox Grid.Row="5" Grid.Column="1" IsChecked="{Binding IsActive}" 
                  VerticalAlignment="Center" Margin="0,5"/>
        
        <!-- Highlight Color -->
        <Label Grid.Row="6" Grid.Column="0" Content="Highlight Color:" Margin="0,5" VerticalAlignment="Top"/>
        <StackPanel Grid.Row="6" Grid.Column="1" Margin="0,5">
            <Rectangle Height="20" Width="100" HorizontalAlignment="Left" Margin="0,5">
                <Rectangle.Fill>
                    <SolidColorBrush Color="{Binding HighlightColor}"/>
                </Rectangle.Fill>
            </Rectangle>
            <Button Content="Change Color" Margin="0,5" HorizontalAlignment="Left" Width="100" 
                    Click="ChangeColorButton_Click"/>
            
            <Label Content="Opacity:"/>
            <Slider Minimum="0" Maximum="1" Value="{Binding HighlightOpacity}" 
                    TickFrequency="0.1" TickPlacement="BottomRight" 
                    HorizontalAlignment="Left" Width="200"/>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="80" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 