<Window x:Class="ScreenRegionProtector.Views.ApplicationEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScreenRegionProtector.Views"
        mc:Ignorable="d"
        Title="Edit Application" Height="300" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Title Pattern -->
        <Label Grid.Row="0" Grid.Column="0" Content="Title Pattern:" Margin="0,5"/>
        <Grid Grid.Row="0" Grid.Column="1" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding TitlePattern}" />
        </Grid>
        
        <!-- Enable Status -->
        <Label Grid.Row="1" Grid.Column="0" Content="Enable Rule:" Margin="0,5"/>
        <CheckBox Grid.Row="1" Grid.Column="1" IsChecked="{Binding IsActive}" 
                  VerticalAlignment="Center" Margin="0,5"
                  ToolTip="When enabled, this application will be restricted to its assigned monitor"/>
        
        <!-- Monitor Restriction -->
        <Label Grid.Row="2" Grid.Column="0" Content="Restrict to Monitor:" Margin="0,5"/>
        <Grid Grid.Row="2" Grid.Column="1" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" Name="MonitorComboBox" 
                      FontSize="14" />
        </Grid>
        
        <!-- Pattern Help -->
        <Label Grid.Row="3" Grid.Column="0" Content="Help:" Margin="0,5" VerticalAlignment="Top"/>
        <TextBlock Grid.Row="3" Grid.Column="1" Margin="0,10" TextWrapping="Wrap">
            Use * as a wildcard to match any part of a window title.
            Example: *Chrome* will match any window with "Chrome" in the title.
        </TextBlock>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="80" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 